#!/bin/bash
# Quick helper script to make all the scripts executable

chmod +x build-claude.sh utils.sh dependencies.sh build-module.sh packaging.sh make-executable.sh install.sh
echo "All scripts are now executable!"
